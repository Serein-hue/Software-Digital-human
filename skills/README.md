# Skills — 开发辅助工具

项目用到的脚本工具，放在项目根目录用 `bash skills/<脚本名>` 或 `python skills/<脚本名>` 执行。

| 文件 | 用途 |
|------|------|
| `deploy.sh` | 构建前端并部署到 GitHub Pages |
| `start-dev.sh` | 一键启动全部开发服务（RAG + 前端） |
| `test_lipsync.py` | 嘴型同步测试工具（不依赖 Fay） |
| `remote_audio_key0.py` | Fay 远程音频输入（按住 0 键拾音发送） |
